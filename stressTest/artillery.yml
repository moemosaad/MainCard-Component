config:
  target: "http://localhost:8080/"
  phases:
    - duration: 60
      arrivalRate: 1000
  processor: "./generateRandomData.js"

scenarios:
  - flow:
      - function: "generateRandomData"
      - get:
          url: "/?{{ id }}"
